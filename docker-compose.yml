version: '3.8'

services:
  novikash-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://mahizuconnectics:servicePONDIKPA2025@novikash-novi-l42sq2:5432/postgres
      - SECRET_KEY=novikash-production-secret-key-2026
      - ALGORITHM=HS256
      - ACCESS_TOKEN_EXPIRE_MINUTES=30
      - LOAN_INTEREST_RATE=0.1
      - DEFAULT_REPAYMENT_DAYS=7
    volumes:
      - uploads_data:/app/uploads
    restart: unless-stopped

volumes:
  uploads_data: